{{ form_start(form, {'attr': {'id': 'championship'}}) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
<div class="btn-group">
    <button form="{{ form.vars.id }}"
            class="btn btn-primary">{% if form.vars.data.id %}{{ "btn.edit"|trans }}{% else %}{{ "btn.create"|trans }}{% endif %}</button>
    {% if form.vars.data.id %}
        {% include 'championship/_delete_form.html.twig' with {"form":form} %}
    {% endif %}
    <a class="btn btn-warning" href="{{ path('app_championship_index') }}">{{ "btn.back"|trans }}</a>
</div>