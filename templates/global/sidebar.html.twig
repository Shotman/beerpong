<div class="offcanvas offcanvas-end bg-body-tertiary" tabindex="-1" id="offcanvasMenu" data-bs-backdrop="false" aria-labelledby="offcanvasMenuLabel">
    <div class="offcanvas-header">
        <h5 class="offcanvas-title" id="offcanvasMenuLabel">Menu</h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body">
        <nav class="navbar navbar-expand-lg navbar-dark">
            <ul class="navbar-nav flex-column">
                {% if is_granted("ROLE_SUPER_ADMIN") %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('app_admin') }}">{{ "Administration"|trans }}</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('app_logout') }}">{{ "Se déconnecter"|trans }}</a>
                </li>
            </ul>
        </nav>
    </div>
</div>